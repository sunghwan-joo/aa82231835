apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "82231835-ingress"
spec:
  ingressClassName: ingress-internal
  rules:
  # 사번으로 변경하세요
  - host: "82231835.cepg-aa.kubepia.net"
    http:
      paths:
      - path: /
        backend:
          service:
            name: troubleshooting
            port:
              number: 8080
        pathType: Prefix
